[project]
name = "connector-bybit"
version = "0.1.0"
description = "Async public REST client for Bybit spot and futures market data"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27,<1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"